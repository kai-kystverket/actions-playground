name: Build and push
on:
  workflow_call:
    inputs:
      context:
        description: 'directory to use'
        required: true
        type: string
jobs:
  build-test:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - name: build
        run: |
          echo building $CONTEXT-$ENV
          exit 1
        env:
          CONTEXT: ${{ inputs.context }}
          ENV: ${{ github.env }}
